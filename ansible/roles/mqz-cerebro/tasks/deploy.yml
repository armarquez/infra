---
- name: Bring up compose stack
  community.docker.docker_compose_v2:
    project_src: "{{ cerebro_compose_dir }}"
    files:
      - compose.yaml
    state: present
    pull: missing
    remove_orphans: true
