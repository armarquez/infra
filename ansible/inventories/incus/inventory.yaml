all:
  children:
    phoenix:
      hosts:
        phoenix-host:
          ansible_host: phoenix
          ansible_connection: community.general.incus
          ansible_incus_remote: incus-remote
          ansible_become: no
