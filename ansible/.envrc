# ./ansible/.envrc
# Ansible Environment Setup

log_status "Loading Ansible environment (Python 3.8)..." 

layout pyenv

# Set Ansible-specific environment variables
export ANSIBLE_HOME=$PWD
# export ANSIBLE_CONFIG="./ansible.cfg"
# export ANSIBLE_INVENTORY="./inventory/"
# export ANSIBLE_ROLES_PATH="./roles:../common_roles" # Example shared roles
# export ANSIBLE_COLLECTIONS_PATHS="./collections:~/.ansible/collections:/usr/share/ansible/collections"
# export ANSIBLE_VAULT_PASSWORD_FILE="~/.ansible_vault_pass.txt"

