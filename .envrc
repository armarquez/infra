# ./.envrc
# Project Root .envrc

export PROJECT_ROOT=$(pwd)

# If you have scripts in the root that require a specific Python environment,
# you could use your custom function here. For example:
# echo "Loading root Python 3.x environment..."
# use_python 3.x # Replace 3.x with your desired general-purpose Python version

log_status "Project root .envrc loaded. Tool-specific environments will be loaded via 'direnv exec' by just."