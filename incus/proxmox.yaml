---
image:
  distribution: proxmox
  release: 8.3
  architecture: amd64
  name: "{{ image.distribution }}-{{ image.release }}-{{ image.variant }}-{{ image.serial }}"
  description: "Proxmox {{ image.release }} ({{ image.variant }}-{{ image.serial }})"
  variant: default

source:
